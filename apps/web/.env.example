# Leave empty to use same-origin (`/api` is proxied to the backend in dev).
# Set this when the backend runs on another host in production.
VITE_API_URL=
# Dev-only: where the Vite `/api` proxy points.
VITE_API_PROXY_TARGET=http://localhost:4000
